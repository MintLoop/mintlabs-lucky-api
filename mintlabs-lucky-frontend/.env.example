# Environment variables for MintLabs Lucky Numbers
# Copy this file to .env and fill in your values

# API Configuration
# For local development (defaults to local API):
# PUBLIC_API_BASE=http://localhost:8000
#
# For Preview / Production on Vercel we recommend using a team secret:
# 1) Add a team secret named `api_base_url` with the API URL (e.g., https://mintlabs-lucky-api.vercel.app)
#    CLI: `vercel secrets add api_base_url https://mintlabs-lucky-api.vercel.app`
# 2) In your Vercel Project → Environment Variables set `PUBLIC_API_BASE` to `@api_base_url` for Preview & Production
#
PUBLIC_API_BASE=http://localhost:8000

# AdSense Configuration (only needed for production)
# PUBLIC_ADSENSE_CLIENT=ca-pub-XXXXXXXXXXXXXXXX

# Note: Remove PUBLIC_ prefix for server-side only variables
# ADSENSE_CLIENT=ca-pub-XXXXXXXXXXXXXXXX
